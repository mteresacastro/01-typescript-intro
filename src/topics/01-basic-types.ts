let name: string = 'Yin';

let hpPoints: number|string  = 96;
// cuando es mas de un tipo se declaran con |. no solo se declara el tipo de dato, también se puede asignar valor fijo, ej. number | 'FULL'.

const isAlive: boolean = true;


console.log({
    name, hpPoints, isAlive
})








export{};